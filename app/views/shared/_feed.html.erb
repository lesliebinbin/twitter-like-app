<% if @feed_items.any? %>
<ol class="microposts">
    <%= render CustomPostComponent.with_collection(@feed_items) %>
</ol>
<%= will_paginate @feed_items, params: {controller: :static_pages, action: :home} %>
<% end %>
